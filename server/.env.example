# Server environment (NEVER commit real secrets — see .env.example)
DATABASE_URL=postgres://user:password@localhost:5432/plantneeds
JWT_SECRET=replace-with-a-long-random-secret
PORT=3001
NODE_ENV=development
